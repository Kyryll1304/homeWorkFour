const x = parseFloat(
  prompt("Введіть кількість годин для конвертації у секунди", "")
);
const y = x * 3600;
if (isNaN(x)) {
  alert("Потрібно ввести число");
} else {
  alert(x + " годин(-а/-и) дорівнює " + y + " секунд(-ам/-и)");
}
